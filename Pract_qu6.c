//read the value of m and display the n=1 when m is larger than 0,0 when m is 0 and -1 when m is less than 0.
#include<stdio.h>
int main()
{
    int m,n;
    printf("the value of m is:");
    scanf("%d",&m);
    if(m!=0)
    if(m>0)
    n=1;
    else
    n=-1;
    else 
    n=0;
    printf("enter the value of m=%d \n",m);
    printf("enter the value of n=%d \n",n);
}
