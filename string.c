//find capital letter in a strings using recursion
#include<stdio.h>
char find(char*);
int main()
{
    char str[]="Hello sir";
    char a = find(str);
    if(a==0)
    printf("no upper case letter");
    else
    printf("%c",a);
    return 0;
}
char find(char *str)
{
    int i=0;
    

}

